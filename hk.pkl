amends "package://github.com/jdx/hk/releases/download/v1.53.0/hk@1.53.0#/Config.pkl"
import "package://github.com/jdx/hk/releases/download/v1.53.0/hk@1.53.0#/Builtins.pkl"

local linters = new Mapping<String, Step> {
    ["trailing-whitespace"] = Builtins.trailing_whitespace
    ["mixed-line-ending"] = Builtins.mixed_line_ending
    ["newlines"] = Builtins.newlines

    ["oxfmt"] = Builtins.oxfmt {
        check = "pnpm run fmt --check --no-error-on-unmatched-pattern {{ files }}"
        check_list_files = "pnpm run fmt --list-different --no-error-on-unmatched-pattern {{ files }}"
        fix = "pnpm run fmt --write --no-error-on-unmatched-pattern {{ files }}"
    }
    ["ox-lint"] = Builtins.ox_lint
    ["tombi-format"] = (Builtins.tombi_format) {
        exclude = List("*.lock")
    }
}

hooks {
    ["pre-commit"] {
        fix = true
        stash = "git"
        steps = linters
    }
    ["fix"] {
        fix = true
        steps = linters
    }
    ["check"] {
        steps = linters
    }
}
